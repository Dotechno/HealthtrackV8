type Vendor @model @auth(rules: [{allow: private}]) {
  id: ID!
  vendorName: String
  vendorAddress: String
  typeOfEquipment: String
  preferredVendor: Boolean
}

type Todo @model @auth(rules: [{allow: private}]) {
  id: ID!
  name: String!
  description: String
}
 